<label for={{type}} class="input-row">
	<input
		class="main-input"
		type={{type}}
		name={{type}}
		id={{type}}
		placeholder={{placeholder}}
	/>
	<span class="label-text">{{placeholder}}</span>
	<span class="invalid-text">{{errorText}}</span>
</label>
